# Проект «Строй-Сейл» — сайт стройматериалов

Интернет-каталог стройматериалов (тротуарная плитка, облицовочный кирпич, газоблок, профнастил, штакетник, сыпучие). Цель — заявки/звонки с таргета VK Реклама и Яндекс Директ. Аудитория: частные застройщики, прорабы, бригады (Россия).

## Пользователь
Новичок без опыта в коде и дизайне. Claude выполняет роли дизайнера и кодера полностью. Объяснять простым языком, по шагам, без жаргона. Решения предлагать готовыми с рекомендацией.

## ЗАКОНЫ ДИЗАЙНА (обязательны в каждой сессии)
1. Перед любой работой над дизайном/версткой ОБЯЗАТЕЛЬНО использовать скиллы:
   - `frontend-design` — официальный скилл Anthropic; задаёт «точку зрения», банит generic-шрифты и AI-штампы, требует «signature element»;
   - `ui-ux-pro-max` — подбор стиля, палитры, шрифтов, UX-правил;
   - `hallmark` — контроль качества «не выглядит сгенерированным» (audit после вёрстки);
   - `banner-design` — для акционных баннеров и рекламных креативов.
2. НИКАКИХ эмодзи в интерфейсе — только SVG-иконки (стиль Lucide, stroke).
3. Шрифты только с поддержкой кириллицы.
4. Mobile-first: трафик с таргета в основном мобильный. Каждую правку проверять на 375px и 1280px.
5. После правок вёрстки — проверка скриншотом через preview-сервер (launch.json → `prototype`, порт 8765).

## РАБОЧИЙ ПРОЦЕСС «сайт за $10k» (по гайду Metics Media, адаптирован)
Порядок для любого крупного редизайна:
1. **Референсы прежде всего.** До вёрстки взять 3–5 скриншотов/ссылок сайтов, которые нравятся пользователю (Dribbble, Awwwards, Pinterest, реальные производители стройматериалов). Прогнать через `hallmark study <URL>` → извлечь ДНК (структура, шрифты, цвета). Без референсов результат всегда «дешёвый» — это главный недостающий вход.
2. **Бриф + уточняющие вопросы.** Перед сборкой задать пользователю уточняющие вопросы и предложить 2–3 конкретных стилевых направления на выбор (как в видео). Не начинать вёрстку, пока направление не выбрано.
3. **Грейдинг по 8 столпам $10k-сайта** (проверять ПОСЛЕ каждого крупного этапа, честно):
   `point of view · typography · colour · hierarchy · imagery · motion · mobile · «невидимое» (скорость/отполированность)`.
   Группы: вкус / содержание / ощущение качества.
4. **Полировка: «дороже, а не пестрее».** Правки просить пачками через ИНТЕНТ, не через перечень («нужны рукотворные микровзаимодействия, нижние секции пресноваты — не делать пестрее, сделать дороже»), а не «добавь тень сюда».
5. **Картинки/видео.** Лучшее — реальные фото товара от заказчика. Нет фото → AI-генерация (ChatGPT image / Вео через агрегатор). Промпт для картинки просить у самого Claude — он знает проект.
6. **Готовые компоненты — 21st.dev** (бесплатно): анимации, скролл-эффекты, hero. Копировать «prompt» с компонента и отдавать Claude. ВАЖНО: прототип — статический HTML, React-компоненты не вставлять как есть (Claude сам отклонит и перепишет под ванильный HTML).

## Скиллы и MCP в проекте (актуальный статус)
- Скиллы (глобально `~/.claude/skills`, подхватываются сами): `frontend-design` (Anthropic), `ui-ux-pro-max`, `hallmark`, `banner-design`, `brand`, `design`, `design-system`, `ui-styling`, `slides`.
- Скиллы, добавленные 07.07.2026 из разбора ютуб-видео (AI LABS + Jack Roberts):
  - Анимации GSAP: `gsap-core`, `gsap-scrolltrigger` (скролл/параллакс/пиннинг), `gsap-timeline`, `gsap-react` (для Next.js).
  - Эстетика (taste-skill, leonxlnx): `design-taste-frontend` (анти-слоп лендинги), `industrial-brutalist-ui` (индустриальный стиль — подходит стройке), `minimalist-ui` (тёплый редакторский минимализм), `redesign-existing-projects` (аудит+апгрейд существующего), `image-to-code` (сначала генерит картинку-дизайн, потом верстает под неё).
- MCP (`.mcp.json`): `shadcn` (официальный) + `github` (поиск чужого кода) + `@21st-dev/magic` (ПОДКЛЮЧЁН 07.07.2026 — генерация premium-компонентов через `/ui`) + `firecrawl` (ПОДКЛЮЧЁН 07.07.2026 — скрейп/анализ сайтов конкурентов, ур.6). better-design — по запросу.
- MCP из видео, требующие БЕСПЛАТНЫХ ключей пользователя (пока НЕ подключены): OpenArt/Higgsfield (генерация фото/видео товара в процессе сборки, ур.4). Подключить по запросу.
- Хостинг из видео (Hostinger) НЕ используем — у нас российский хостинг (см. ниже). Остальной рецепт видео применяем целиком.

## Дизайн-инструменты (вкус)
- `.mcp.json` подключает **shadcn MCP** (официальный, бесплатный) — использовать для компонентов в боевой Next.js-версии.
- **better-design** (github: marvkr/better-design, MIT) — 31 дизайн-система уровня Linear/Stripe/Apple с токенами и кодом компонентов. Читать напрямую через `mcp__github__get_file_contents` (components/<system>/, registry/<system>/). Для стройки смотреть: Light Marketplace, Precision Light, Monochrome Industrial. Облачный MCP требует бесплатный ключ с better-design.com/settings — если пользователь дал ключ, добавить в .mcp.json.
- **hallmark study <URL или скриншот>** — бесплатный аналог Refero: извлекает «ДНК дизайна» (структуру, шрифты, цвета) из любого эталонного сайта. Использовать ПЕРЕД крупным редизайном.
- Эталонная планка качества: не «шаблон с градиентами», а уровень лучших европейских производителей стройматериалов (Vandersanden, Wienerberger) — крупная фотография продукта, сдержанная типографика, много воздуха.

## Правила работы
- Поиск на GitHub — только через MCP-инструменты `mcp__github__*` (не веб-поиск).
- Актуальную инфу по сервисам/технологиям проверять веб-поиском, не отвечать по памяти.
- Отчёты и объяснения — на русском.

## Технические решения (утверждены, не пересматривать без запроса)
- Сайт: статические HTML/CSS/JS в КОРНЕ проекта (~/Desktop/сайт, index.html двойным кликом) — с 08.07.2026 это основной проект, папки prototype/ больше нет. Упоминания prototype/ в старых записях статуса читать как корень.
- Боевая версия: Next.js + Tailwind + shadcn/ui (+ Framer Motion для анимаций).
- Хостинг: российский (Beget/Timeweb/Яндекс Cloud). Vercel НЕ использовать — нестабилен из РФ.
- Заявки: форма → Telegram-бот (мгновенно) + amoCRM (сделка). Без регистрации и личных кабинетов.
- Аналитика: Яндекс.Метрика + пиксель VK. Цели на все формы и звонки.
- Supabase/БД на старте не нужны — «базой» служит amoCRM.

## Требования из ТЗ (переписка с заказчиком)
- Корзина без регистрации; заказ = звонок менеджера за 5 минут; оплата при получении.
- Калькуляторы: кирпич (стены), кровля, газоблок; плитка по площади в карточке товара.
- Акционные баннеры: задаётся срок действия, после даты баннер исчезает сам.
- Характеристики товара — простым языком «где используется», без перегруза таблицами.
- Единый телефон в шапке + «перезвоним за 5 минут».
- Реклама ведётся на страницы категорий, не на главную.

## Статус (обновлять при крупных шагах)
- 06.07.2026: ПОЛНЫЙ РЕДИЗАЙН. Старый прототип удалён, все данные сохранены в ДАННЫЕ-КОМПАНИИ.md.
- Референсы утверждены: Petersen Tegl (petersen-tegl.dk) + Fireclay Tile (fireclaytile.com/collections/natural-press). Стиль берём полностью оттуда.
- 07.07.2026: СМЕНА ФОКУСА под Яндекс Директ. Каталог сужен до 4 главных: облицовочный кирпич · обычный кирпич · тротуарная плитка · кровля (металлочерепица + профнастил вместе). Остальное (газоблок/штакетник/сайдинг/сыпучие) — блок «Ещё возим» внизу главной.
- 07.07.2026: Фирменная фишка главной — интерактивная шапка «дом на участке»: 4 терракотовые точки-маркера над фото → ссылки в категории. Десктоп — подпись по наведению; мобильный — те же 4 пункта списком-кнопками (.hero-legend). Собрано в prototype/index.html + styles.css, проверено на 375/1280. Фото пока заглушка (нужен промпт 1 nano banana).
- Этап 1 (текущий): макет главной ГОТОВ (заглушки фото). Дальше: промпты nano banana (дом + 4 обложки) → страницы категорий (реклама ведёт на них) → карточка/корзина/калькуляторы.
- 07.07.2026 (вечер): ПЛАН КАТАЛОГА КИРПИЧА утверждён — см. КАТАЛОГ-ПЛАН.md. Данные: ~/Desktop/фото/ (5 поставщиков, ~290 товаров). Принцип «коллекций»: 1 поставщик = 1 коллекция со своим именем, имена поставщиков скрыты, фото разных стилей не смешиваются в одной сетке. Славянский без цен → «Узнать цену». Шапка главной — 3 готовых рендера из ~/Desktop/фото/ШАПКА КАРУСЕЛЬ/ (вместо nano banana).
- 08.07.2026: БАЗА ТОВАРОВ ГОТОВА. tools/parse_catalog.py → prototype/data/catalog.json (274 товара: Формовка 100, Палитра 54, Классика 44, Европа 32, Эконом 27, обычный 17; у 34 Донских нет фото; 28 «по запросу»; 35 с видео). Проверочная таблица КАТАЛОГ-ТАБЛИЦА.xlsx (лист Каталог + Сводка; формулы пересчитает Numbers/Excel при открытии — LibreOffice на Маке нет). При изменении цен: править txt → перезапустить оба скрипта.
- 08.07.2026: Шапка главной СОБРАНА на рендере 03 (hero-03.jpg 1760px, prototype/img/): десктоп — текст слева + дом справа (grid 5fr/7fr, всё в 1-м экране), мобайл — фото + легенда-кнопки. 4 маркера: крыша 44/22, стена 56/62, цоколь 30/81, дорожка 20/97 (% от 16:9). Проверено 375/1280. Рендеры 01/02 в запасе (кровля/баннеры).
- 08.07.2026 (ночь): ПРОДАЮЩИЙ ПРОХОД по главной (Trust & Authority из ui-ux-pro-max): полоса доверия (4 SVG-пункта: 5 минут/доставка/оплата при получении/с заводов Юга), реальные обложки категорий (cat-oblic=Бромо кладка, cat-brick=Классика кладка, cat-tile=фото заказчика, cat-roof=кроп рендера 02), цены «от» на карточках (обл. от 20,50 ₽ · обычный от 12 ₽ · плитка от 1 105 ₽/м² ДЕМО · кровля «расчёт»), согласие 152-ФЗ в форме + policy.html (реквизиты TODO), тап-зоны точек 44px, стаггер появления точек. КАРУСЕЛЬ В HERO НЕ ДЕЛАЕМ (осознанно: автокарусели снижают конверсию, дом-точки — фирменная фишка; рендер 02 ушёл в обложку кровли, 01 — в запасе на баннеры).
- 08.07.2026: HALLMARK-АУДИТ ПРОЙДЕН (0 критичных). Исправлено: легенда — 1 колонка на мобиле (кнопки без переносов), контраст мелкого текста ink-soft, сноска «цены стартовые от завода» под каталогом, «цены ниже рынка»→«без посредников». Осталось мелкое: 2 инлайн-стиля (promo-кнопка, max-width формы), повтор «за 5 минут» ×4, карточки каталога ведут на #catalog (страниц категорий ещё нет).
- 08.07.2026: КАТАЛОГ ОБЛИЦОВОЧНОГО ГОТОВ. Исследование мировых каталогов через firecrawl (Vandersanden BE, Glen-Gery US, Brickhunter UK): подтверждено — фильтры «цвет→фактура→формат», цвет свотчами, группировка по коллекциям, цена на карточке. Собрано: tools/build_images.py (233 фото → prototype/img/catalog/, 4:3 640×480, прозрачные PNG на подложке; у «Палитры» срезаны нижние 14% кадра — там водяной знак clideo) + tools/build_category.py (kirpich-oblitsovochnyy.html — 8 свотчей подбора по цвету + 5 лент по 4 карточки; collection-*.html ×5 — сетка + фильтры цвет/фактура/формат + «Показано N из M» + предустановка ?color= из ссылки «Ещё N — в коллекции»). При изменении данных перезапустить оба скрипта. Багфиксы: [hidden]{display:none!important} (display:flex карточек перебивал hidden — показывались все 257), .lanes minmax(0,1fr) + короткий текст кнопки (лента распиралась до 397px на 375), .btn min-height 44px, контраст swatch-n/p-none → ink-soft. Цена на главной «от 20,50»→«от 18,50 ₽/шт» (реальный минимум прайса — Персик Тербунский). Точка на доме, легенда и карточка ведут на страницу категории. Проверено на эмуляции 375×812 и 1280×800: hscroll нет, тап-зоны ≥44px, консоль чистая. У 24 Донских без фото — заглушка «Фото пришлём по запросу».
- 08.07.2026: КАТЕГОРИЯ ПЕРЕИМЕНОВАНА «Обычный кирпич» → «Забутовочный кирпич» (везде: главная, точка на доме, легенда, xlsx). Страница kirpich-zabutovochnyy.html СОБРАНА: подбор по задаче («Фундамент и цоколь» → полнотелые М150 · «Несущие стены» → М125+ · «Перегородки и хозпостройки» → пустотелые/М100/некондиция), 2 ленты (Классика=Донской 13 после скрытия дублей-фасовок rab-007/009, Палитра=Губский 2), имена почищены (RAB_VIEW в build_category.py — имя/мета/задачи руками). ЦЕНЫ СКРЫТЫ по просьбе пользователя — все карточки «Узнать цену», на главной «цена по запросу»; ПОЛЬЗОВАТЕЛЬ ПРИШЛЁТ ПРАЙС ПОЗЖЕ (в базе цены Донского есть — при получении прайса обновить txt → parse_catalog → снять price_off). Проверено на 375: hscroll нет, фильтр/сброс работают, легенда однострочная.
- 08.07.2026: САЙТ ПЕРЕЕХАЛ ИЗ prototype/ В КОРЕНЬ проекта (основной проект по решению пользователя). Обновлены пути в tools/*.py и .claude/launch.json (сервер раздаёт корень, имя конфига осталось «prototype»). ЛИПКАЯ ПОЛОСА СВЯЗИ (.callbar, только мобайл <900px): Позвонить (терракота) + WhatsApp + Telegram + MAX, тап-зоны 48px, body padding-bottom; на всех страницах (в генераторе — константа CALLBAR в build_category.py, на index/policy вставлена руками). TODO перед запуском: реальные ссылки wa.me/t.me/max.ru. КАЛЬКУЛЯТОР КИРПИЧА на странице облицовочного (#calc): площадь − проёмы, форматы 1НФ/евро (51,4 шт/м² — расход из спеков Губского) и 1,4НФ (39,2 — по габаритам ГОСТ), запас 5%, округление до десятка, CTA «Получить точный расчёт» → форма; формовка/лонг — «посчитает менеджер». Проверено: 375 (полоска, калькулятор 120−18 → 5 510 шт, hscroll нет) и 1280 (полоска скрыта). Консоль чистая.
- 08.07.2026: КАТАЛОГ ТРОТУАРНОЙ ПЛИТКИ ГОТОВ (поставщик Легион, zavod-legion.ru — имя на сайте скрыто). Данные: ~/Desktop/фото/Тротурная плитка/ → tools/parse_tiles.py → data/tiles.json (172 товара: 7 форм × ~24 цвета, от 650 ₽/м²; 2 бордюра 560/240 ₽/шт; 13 без фото — заглушка). ФОТО: у рендеров/текстур водяной знак LEGION по центру → tools/build_tile_images.py берёт чистый УГЛОВОЙ кроп текстуры (верхний левый, 40% высоты, 4:3) → img/catalog/tile-*.jpg; обложки форм из Категории/ чистые (600×600); герой — plitka.webp/plitka2.webp (чистые рендеры без знака) → img/plitka/hero-tiles-*.webp; галерея «Наши работы» — 10 фото отобраны по контактному листу (без штампов камеры) + видео с производства 3,6МБ (ffmpeg-постер). СТРАНИЦЫ: tools/build_tiles.py → trotuarnaya-plitka.html (герой с CSS-анимацией tile-drift «плитка дышит» — референс zavod-legion.ru; 4 факта простым языком; 7 форм; бордюры; калькулятор м²→поддоны→стоимость; работы+видео) + plitka-<форма>.html ×7 (фильтр Все/Колормиксы/Однотонные, мини-калькулятор). Главная: карточка/точка/легенда → trotuarnaya-plitka.html («от 650 ₽/м²»), промо-баннер → plitka-staryy-gorod.html. xlsx: лист «Плитка» (174 позиции). Проверено 375/1280: фильтры, калькуляторы (60→63 м²·5 поддонов·40 950 ₽), hscroll нет, консоль чистая. При изменении данных: parse_tiles → build_tile_images → build_tiles → make_xlsx.
- 08.07.2026 (2): КАРТОЧКИ ТОВАРА ПЛИТКИ ГОТОВЫ — tovar/<форма>-<цвет>.html ×172 (галерея 2–3 фото с переключением, цена ₽/м², характеристики простым языком из txt поставщика (40мм/F200/B30/поддон 15 м²·1,4т/фура 225 м²), «куда берут» по форме, калькулятор с ценой товара, 4 похожие расцветки, JSON-LD Product; ссылки из tovar/ с префиксом ../). Карточки в сетках форм — ссылки на товар. ФОТО ДОГРУЖЕНЫ ВСЕ (было 13 без фото → 0): parse_tiles.py теперь читает jpg/jpeg/png+webp (раньше только webp), рендер/текстура определяется по периметру кадра (белая плитка больше не путается с рендером), 41 чистая текстура без водяного знака размечена глазами → tools/clean_textures.json (идут целиком+зум), у остальных знак LEGION по центру → в галерею идут верхняя (0–40%) и нижняя (62–100%) полосы кадра. Рендеры с логотипом на сайт НЕ идут (поставщик скрыт), сертификаты тоже (на них имя завода). ПУСТОТЫ УБРАНЫ: 8-я плитка «Бордюры» в сетке форм (было 7 в сетке 4×2 — дыра), бордюрные фото на белом холсте с волосяной рамкой + сетка бордюров ограничена 880px, видео-секция перевёрстана (3 факта производства на волосяных линиях, wv-facts), «Добавьте в корзину» на главной заменено (корзины ещё нет). Пересборка: parse_tiles → build_tile_images → build_tiles → make_xlsx. Проверено 375/1280: hscroll нет, консоль чистая, 189 страниц — битых ссылок/якорей 0.
- ФОТО «НА ДОМЕ» (следующий контент-шаг): пользователь собирает у поставщиков фото готовых домов/заборов по цветам; недостающее — nano banana по workflow из памяти (референс + промпт блоками; решение пользователя: базовые дома искать в Pinterest, материал менять нано-бананой). Складывать в ~/Desktop/фото/ФАСАДЫ/<коллекция>/<имя товара как в каталоге>/*.jpg — Claude подключит в карточку товара.
- 09.07.2026: БОЛЬШОЙ ПРОХОД ПО КАЧЕСТВУ (аудит + референсы Braer/Vandersanden через firecrawl). Калькулятор переделан в «строку сметы» (.line-calc): одна строка под карточкой товара, виден на 1280×800 без скролла, на мобиле складывается; починены НЕСУЩЕСТВУЮЩИЕ токены --text-2xs/3xs (из-за них подписи рендерились 16px и распирали мобильную карточку до 504px). ВОДЯНЫЕ ЗНАКИ: в build_tile_images.py кто-то заменил band-crop на полные кадры — LEGION ушёл на сайт; восстановлено (чистые → кадр+зум, со знаком → полосы 0–40%/62–100%, рендеры исключены — у них тоже знак). clideo-срез в build_images.py теперь по dir (Губский/), а не по supplier — 8 товаров Классики были со знаком. Жаргон убран: «Выберите цвет фасада», форматы «одинарный/полуторный» вместо 1НФ (FMT_SHORT в build_category.py). Карточки лент кирпича — ссылки в коллекцию с ?color= пресетом. Ритм: --space-3xl 128→96px, .p-img в волосяной рамке, --color-surface вместо #fff. Кэш: styles.css?v=4, img ?v=3. Проверено 375/1280, 189 страниц, битых ссылок 0, консоль чистая.
- 09.07.2026 (2): ЦВЕТ + МАРКЕТИНГ (референсы: Cambridge/Nicolock/Unilock/EP Henry — у всех цветной CTA и lifestyle-фото). Кнопки .btn → терракота --color-btn oklch(50% 0.115 42), hover 42% (контраст 6,3:1); полоса доверия и line-calc-note на тёплой подложке --color-tint; финальный CTA-блок .cta-band — тёмная ink-панель (заголовки продающие, у tovar «Возьмём подбор на себя»). КОНВЕРСИОННЫЙ БАГ починен: кнопка калькулятора «Получить точный расчёт» скроллила к #lead, которого НЕ БЫЛО на сгенерированных страницах (мертва на всех 186) — теперь это <a href="#lead">, id=lead стоит на cta-band, html{scroll-behavior:smooth}+reduced-motion. Тексты главной: «цены с доставкой» (ложь) → «440+ товаров», hero-sub продаёт заводы Юга, «К плитке» → «Выбрать со скидкой», бейдж «Демо-акция» скрыт в коммент, секция «Наши работы» (4 фото work-*.jpg) перед «Как заказать». Кэш v=5. ВАЖНО про headless-превью: плавный скролл в нём не проигрывается — проверять якоря мгновенным scrollIntoView, это не баг сайта.
- MCP better-design ПОДКЛЮЧЁН 09.07 (ключ в ~/.claude.json, user-scope) — доступен со следующей сессии. Ключ GEMINI (nano banana) в tools/.env (gitignored) — для генерации «фото на доме».
- Ждём от пользователя: подтверждение города (Краснодар), реальный телефон, username мессенджеров (WhatsApp/Telegram/MAX), ПРАЙС на забутовочный, финальные цены облицовочного (наценка на заводские!), фото кладки/домов.
