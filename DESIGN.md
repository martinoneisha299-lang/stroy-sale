# DESIGN.md — манифест стиля «Стройсейл»

Единый источник правды по внешнему виду сайта. Читать ПЕРЕД любой правкой
вёрстки: без него страницы разъезжаются между разделами.

Редакция от 29.07.2026 — редизайн «витрина магазина». Предыдущая редакция
(музейная типографика, нулевые скругления, обложки-карусели) отменена целиком:
если встретите в старых записях `.pb*`, `.pick-bar`, `.swatch`, `.line-calc`,
`.cta-band`, `.callbar` — это мёртвые классы, их больше нет.

---

## 1. Точка зрения

Мы не «красивый лендинг про стройку». Мы **магазин, в котором материал считают**,
а не просто продают. Отсюда три вещи, которых нет у конкурентов и которые нельзя
выкидывать при полировке:

1. **Двойная цена.** Кирпич — `₽/шт` и рядом `≈ ₽/м² кладки`. Плитка — `₽/м²`
   и `поддон 15 м² ≈ N ₽`. Человек покупает стену и двор, а не штуки.
2. **Образцы — настоящие фотографии.** Кружок цвета в фильтре — это кроп
   реального кирпича (`img/catalog/cdot-<id>.jpg`), а не плоская заливка.
3. **Калькулятор рядом с товаром**, а не отдельной «услугой»: площадь → штуки →
   поддоны → деньги.

Всё остальное сознательно делаем **как у больших магазинов** (Лемана Про,
ВсеИнструменты, Петрович): человек с рекламы должен узнать интерфейс за секунду,
а не разбираться в авторском дизайне. Оригинальность тратим на три пункта выше,
остальное — привычные паттерны.

Макет-первоисточник: `design_handoff_stroysale/` (README + HTML + скриншоты).

---

## 2. Цвет

Правило: **страница белая, поверхности — три ступени серого, цвет живёт только
на действии и на фотографии товара.**

| Роль | Токен | Где |
|---|---|---|
| Фон страницы | `--paper` #ffffff | всё полотно |
| Поля, вторичные кнопки | `--field` #f0efed | поиск, «Показать ещё», чипы |
| Плитки, врезки | `--tile` #f2f0ec | плитки разделов, полоса условий, форма |
| Линии | `--rule` / `--rule-strong` | рамки карточек / рамки контролов |
| Текст | `--ink` → `--ink-2` → `--ink-3` → `--ink-4` | четыре ступени, см. ниже |
| Действие | `--dark` #2f2c29 | главная кнопка «В заявку» |
| Фирменный | `--accent` #c0563e | кнопка каталога, бейдж, кнопка отправки |

**Терракота — акцент, а не заливка.** Её доля на экране ≤ 5 %: кнопка каталога,
бейдж «Новинка», номера шагов, одна кнопка отправки формы — и всё.

**Контраст (посчитан, не менять на глаз):**
`--ink-4` #8c8985 = 3,4:1 — только декоративное (разделители крошек, неактивные
точки). Мелкий текст — не светлее `--ink-3` (5,2:1). Белый на `--accent` = 4,52:1,
проходит AA впритык: на акценте не бывает текста мельче 13px.
Зелёный `--ok` = 3,0:1 — **только иконки**; сам текст наличия всегда `--ink-2`.

Смена фирменного цвета = две строки в `tokens.css`: `--accent` и `--accent-press`.
Если новый цвет светлый (жёлтый, лайм) — обязательно поменять `--on-accent`
на тёмный, иначе белые подписи станут нечитаемыми.

---

## 2а. Форма: скругления, линии, тени

Редакция 06.08.2026. Заказчик: «убрать следы ИИ, лишних округлений нет,
строго и очень красиво». Замеры пяти витрин (Amazon, IKEA, HORNBACH,
Wildberries, Яндекс Маркет) показали то же самое: рынок расколот на два
лагеря — **0–4px** (каталожные: HORNBACH, IKEA, Amazon) и **12–13px**
(маркетплейсы: WB, ЯМ). Промежуточных 6–10px нет ни у кого: это «ничья
земля», куда сваливаются шаблонные сайты. Мы в первом лагере.

**Скругление одно на весь сайт — `--r-ui` 4px**, и оно есть только у того,
что нажимают: кнопки, поля, чипы, бейджи, счётчик, выпадающие списки.

- **Поверхности — прямоугольники.** Карточка товара, панель фильтров, плитка
  раздела, врезка формы, акция, карта: их держат волосяная линия и отступ.
- **Фотографии не скругляются никогда.** Ни в карточке, ни в галерее, ни
  в миниатюре. Кадр — прямоугольник.
- `--r-pill` 999px — только у физически круглых вещей: образец цвета,
  кружок мессенджера, пузырь счётчика. Больше нигде.
- **Одна линия** `--line` 1px. 1.5px у контролов был компромиссом «чтобы
  заметнее»; заметнее делает ЦВЕТ линии (`--rule` → `--rule-strong`).
- **Теней у карточек нет** — ни в покое, ни по наведению. На пяти
  промеренных витринах суммарно 12 теней, и все до одной у выпадающих
  меню и оверлеев; у HORNBACH их ноль на всю страницу категории. Осталось
  два токена: `--sh-pop` (выпадающий список, тост) и `--sh-bar` (липкая
  панель снизу).
- **Наведение — одно изменение.** Карточка и плитка меняют цвет линии, и всё.
  Зум фото, подъём тенью и сдвиг кнопки при нажатии убраны.

Почему это важнее, чем кажется: восемь разных радиусов (8/10/11/12/14/18 +
хардкод 3/5/7/9) читаются как «всё просто скруглено на глаз» — это и есть
главный признак сгенерированного макета. Если новому элементу «нужен свой
радиус», значит лишний он, а не радиус.

---

## 3. Типографика

Один шрифт — **Golos Text** (400/500/600/700/800/900), кириллица родная.

| Токен | px | Роль |
|---|---|---|
| `--fs-micro` | 12 | подписи таб-бара, этикетки |
| `--fs-xs` | 13 | мета карточки, крошки, единицы цены |
| `--fs-sm` | 14 | счётчик выдачи, чипы |
| `--fs-base` | 15 | **интерфейс**: кнопки, имя товара, фильтры |
| `--fs-md` | 16 | абзацы и **все поля ввода** |
| `--fs-lg` | 18 | телефон в шапке |
| `--fs-price` | 20→22 | цена в карточке |
| `--fs-h1` / `--fs-h2` | 22→32 / 20→22 | заголовки |

Жёсткие правила:

- **Поля ввода — не мельче 16px.** На меньшем iOS зумит страницу при фокусе.
- **Ничего мельче 12px.** Исключение — цифры в кружках-счётчиках (11px):
  это цифра, а не текст.
- **Никаких переносов по слогам** в заголовках: `hyphens: manual`. Мы уже ловили
  «произ-водство» и «хозяйствен-ный».
- Заголовки — `text-wrap: balance`, абзацы — `text-wrap: pretty`.
- Неразрывные пробелы ставит `tools/nbsp.py` при записи страницы. Руками
  `&nbsp;` не пишем; в JS-строках — символ ` `.

---

## 4. Пространство и сетка

- Контентная колонка `--wrap` 1216px, боковой отступ на телефоне 16px.
- Отступы — шкала `--s-1…--s-12` (4/8/10/12/14/16/20/24/28/32/40).
- Между секциями `--s-section` (28→48px).
- **Брейкпоинты в em**, не в px: при системном увеличении шрифта страница сама
  уходит в узкую раскладку.
  `30em` карточка дышит · `48em` планшет, таб-бар прячется · `64em` панель
  фильтров слева · `80em` полная сетка макета.
- Сетка товаров: телефон — 2 колонки на волосяных разделителях (линии рисует
  фон сетки, `gap:1px`), от 48em — карточки в волосяных рамках без скругления.
  Минимум колонки задан в `rem` (`minmax(min(9rem, 100%), 1fr)`): rem растёт
  вместе с системным шрифтом, и на 200% сетка сама складывается в одну —
  контейнерный запрос объявлен только у выдачи каталога, а на страницах
  товара его нет.

**Первый экран телефона (правило заказчика, референс kirpich.ru).**
Шапка + герой главной обязаны укладываться так, чтобы **верх карточек каталога
попадал в первый экран** — 600–640px от начала страницы при вьюпорте 812.
Проверять замером, а не на глаз: `node tools/shot.mjs 8765 index.html 393 0 812`.
Отсюда мобильные значения героя (кегль h1 `min(--fs-hero, 6.6vw)`, цифры в три
колонки, одна кнопка, `padding-block: 20px 22px`, `margin-bottom: 0`) — на
планшете и шире всё это возвращается к просторным значениям в блоке `≥48em`.

**Ловушки сеток (ловили не раз):**
- `grid-template-columns: 1fr` раздувается до самого длинного слова —
  всегда `minmax(0, 1fr)`.
- У `auto-fill` минимум оборачиваем в `min(...)`: `minmax(min(15rem, 100%), 1fr)`.
- Модификаторы `.p-grid--cN` обязаны стоять ПОСЛЕ базового правила, иначе
  их перебьёт `auto-fill`.
- `[hidden]` объявлен с `!important`: без этого `display:flex` компонента
  побеждает атрибут, и фильтры показывают скрытые карточки.

---

## 5. Компоненты (канон)

| Компонент | Класс | Правило |
|---|---|---|
| Кнопка действия | `.btn` | «В заявку» ВСЕГДА тёмная — и в карточке витрины, и на странице товара. Акцентом красятся только «применить/отправить» (фильтры, форма) и обводка `.btn--call` «Позвонить» |
| Количество | `.pd-buy` + `.qty` | счётчик рядом с ценой; старт — поддон, если завод его указал; подпись считает м² кладки или поддоны (app.js §9b) |
| Карточка товара | `.p-card` | фото → имя → цена → ₽/м² → наличие → «В заявку» |
| Галерея товара | `.pd-main` | кадр 4:3 (родной формат каталога 1200×900), НЕ квадрат; на десктопе колонка фото ≤32rem — растяжка во всю ширину даёт мыло |
| Шапка листинга | `.page-head` | крошки + h1 и всё. Никаких SEO-абзацев и рядов фактов под h1 (Hornbach/ИКЕА: текст — только внизу страницы, если вообще нужен) |
| Палитра с подписями | `.swl` | сетка «кружок 56px + имя + код»; имена видимы всегда (на таче ховера нет), никаких перечислений имён через «·» абзацем |
| Бейдж | `.p-badge` | только «Новинка» (список ведётся руками). «Хит» не выдумываем |
| Плитка раздела | `.tile` | фото сверху во всю ширину (`order:-1`), под ним имя и счётчик |
| Акция | `.promo` | плашка с текстом + кадр РЯДОМ (38%). Текст поверх фотографии под градиентом — приём рекламного лендинга, у нас его нет |
| Шаг «Как заказать» | `.step` | цифра на линии сверху. Никаких номеров в кружках |
| Таблица характеристик | `.pd-specs dl` | пары «графа — значение» на волосяных линиях |
| Панель фильтров | `.filters` | ОДНА форма на страницу; на телефоне JS переносит её в шторку |
| Шторка | `.drawer` | каталог — слева, фильтры — справа; Esc и клик по фону закрывают |
| Таб-бар | `.tabbar` | 5 **действий**, не разделов: Главная/Каталог/Заявка/Позвонить/Написать. Приклеен к ВИДИМОЙ области: `viewport-fit=cover` + `env(safe-area-inset-bottom)` + сдвиг по `visualViewport` (app.js §12) — иначе во встроенных браузерах он отлипает и лежит под «домашней» чертой |
| Липкая панель товара | `.pd-bar` | на телефоне липнет над таб-баром, на десктопе обычная |
| Фирменный маркер | `.brick-mark` | терракотовый «кирпичик» 9×5 — единственная декоративная форма |

**Что НЕ делаем:**
эмодзи вместо иконок · звёзды рейтинга и отзывы, которых нет · «осталось 3 шт»
и таймеры обратного отсчёта · бесконечный скролл · автолистающиеся карусели ·
четыре разноцветные кнопки мессенджеров в ряд · библиотеки ради карусели,
селекта или лайтбокса.

---

## 6. Движение

`--dur` .15s микроотклик, `--dur-2` .26s шторки. Наведение = ОДНО изменение:
цвет фона или линии. Зум фотографий, подъём карточки тенью и сдвиг кнопки
при нажатии убраны — на промеренных витринах ничего этого нет. Единственная
смена кадра по наведению — второй кадр товара (`.p-alt`, кроссфейд .15s):
это приём магазина, а не украшение. Никаких параллаксов и появлений по
скроллу. `prefers-reduced-motion` гасит всё — правило уже в §0 стилей.

---

## 7. Как собирается сайт

```
data/*.json ─┬─ tools/build_category.py  → кирпич: витрина, 5 коллекций,
             │                             забутовка, 282 страницы товара
             ├─ tools/build_tiles.py     → тротуарная плитка
             ├─ tools/build_roof.py      → кровля, фасад, забор
             └─ tools/build_site.py      → главная, заявка, поиск, акции,
                                           доставка, работы, политика
                    ↓
             tools/build_search.py       → data/search.json (читает готовые страницы)
```

Общее лежит в двух модулях, дублировать их запрещено:
- `tools/shell_common.py` — шапка, меню, футер, таб-бар, шторка каталога,
  карточка товара, контакты, иконки, `page_shell()`;
- `tools/catalog_common.py` — панель управления, фильтры, сетка.

Весь клиентский код — один **`app.js`** в корне. Инлайновых `<script>`
в страницах быть не должно (кроме `ld+json` в `<head>`).

**После любой правки CSS или JS поднять версию кэша** в `shell_common.py`
(`STYLES_V`, `APP_V`) и пересобрать все четыре генератора — иначе у посетителя
останется старый файл и вёрстка поедет.

---

## 8. Чек-лист перед коммитом

1. Все четыре генератора отработали + `build_search.py`.
2. `python3 tools/check_links.py` — ноль битых ссылок, картинок и якорей
   (`?v=N` в пути отрезать перед проверкой).
3. `node tools/scan_responsive.mjs 8765` — ноль находок
   (13 ширин × 4 масштаба системного шрифта).
4. `python3 tools/check_texts.py` — ноль запрещённых формулировок.
5. `node ~/.claude/skills/impeccable/scripts/detect.mjs styles.css index.html …` —
   машинный детектор ИИ-следов (~60 правил; ставится из pbakaus/impeccable,
   56k★). Ложные срабатывания у нас два: `broken-image` на строку CSS
   `:has(img)` и на JS-шаблон, и `em-dash-overuse` — в русском тире
   законная пунктуация, а правило англоязычное.
6. Глазами 375 и 1280: главная, витрина, коллекция, товар, заявка.
7. Консоль браузера чистая.
8. Версии кэша подняты.

**Ловушки проверки:**
- Панель браузера отдаёт устаревший кадр при скролле и `document.hidden = true` —
  скриншоты снимать через `tools/shot.mjs` (headless Chrome, точный вьюпорт).
  В панели ещё и `requestAnimationFrame` заморожен: код, который висит на rAF,
  там «не работает» — это не баг сайта.
- Браузер кэширует `index.html` (у него нет `?v=`), и правки CSS не видно даже
  после поднятия версии. В `shot.mjs`/сканерах включён
  `Network.setCacheDisabled` — при своих проверках делать так же.
- `scrollTo` в проверках — только с `behavior:'instant'`.
- Ширина 375 в headless-хроме через `--window-size` шире реальной: мерить
  через `Emulation.setDeviceMetricsOverride` (в `shot.mjs` уже так).

---

## 9. Что осталось решить с заказчиком

- **Наличие и сроки.** В карточке стоят осторожные формулировки
  («Доставка по краю», «Под заказ»). Когда появятся реальные остатки —
  править `shell_common.AVAIL_*` и `TERMS_*`, это одно место.
- Реальный телефон и ссылки мессенджеров вместо заглушек.
- Наценка на заводские цены; цены забутовочного кирпича сейчас скрыты.
- Приём заявок: форма пока только проверяет телефон — нужен Telegram-бот
  и amoCRM.
- Яндекс.Метрика и пиксель VK.
